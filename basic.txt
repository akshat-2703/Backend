/*var arr = [1,2,3,4] ;
const ans = arr.map(function(val){
    return val*3 ;
})
console.log(ans) ;
console.log(arr);

var arr = [1,2,3,4] ;
var ans = arr.filter(function(val){
    if(val > 3) {return true ;}
    else return false ;
})
console.log(ans) ;
var arr = [1,2,3,4] ;
var ans = arr.find(function(val){
    if(val == 2) return val ;
})
console.log(ans);
console.log (arr.indexOf(12) );
var arr = [1,3,4] ;
function abcd(a,c) {
    
}
console.log(abcd.length);
*/
http and https 
protocol - rules  internet par kuch rne
//npm = node package manager
//node js core mein jo install aata h wo khalata h module
//npm se download krte hai wo hota hai package
//for install and uninstall anything require that is we have //install = npm i packagekanaam
//dependencies = package and packages ki dependencies 
//dev-dependencies =aise package jo sirf development m kaam ayge pr jb app bn jayga deploy hoyga tb use ni krege
//kai baar ap koi script chalate ho and us script mein aap likhte ho as 
npm start 
npm run dev ,npm run conccurent why u write run 
script = start and test  //this is basically when u install npm and node js as install hone k bad node js m operating system m y tunki table m create kr diye the 
asa command jo hmne khud ne create kiya h for them ase command ke liye hmko run lagana padta h aage
run basically we have to find in script
//express js ek npm package hai 
framework basically wo ek particular set aur rules create krta hai and that rules only we have to follow and we have independent how we follwo but we have to follow that only angular bhi ek framework hi h basically 
whi react ek library that provide protocol aur things that provide and its our wish that how e make it nothing any depedenc
as for explain express js basically wo h as we send any request like utube we send request for video and for that request as server basically changes the way as it accepting the request until ssendding the reponse of that request that all will done by express js
//middleware
jab bhi server request accept krta hai waha se route ke beech pahuchne tak agar aap  us request ko beech me rokte ho and kuch perform krte ho ,to ye element middleware khlata h 
//express js k paas ek special route h that for error handling that we have app.use only 


//FORM HANDLING AND WORKING WITH THE FORMS 
//handle backend process of forms and making sure  that the data coming from any frontend library,framework and templating engine ,we still handle it  at the backend 


//SESSION COOKIE  
hum log kcuh bhi data frontend par browser par rkh skte hai and jab bhi aap kuch bhi request backend par karoge wo frontend par saved data automatically backend par chala jaayega

tumne to bheja tha plain text par server ko mila blob which is not directly readable ab is cheej ko handle karna padega ki hum us blob ko waapas se readable kar ske